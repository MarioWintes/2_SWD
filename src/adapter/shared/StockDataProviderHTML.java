package adapter.shared;

public class StockDataProviderHTML {
    public HTMLData bieteHTML() {
        // hier würde normalerweise echtes HTML geladen werden
        return new HTMLData("<data>stock-werte</data>");
    }
}
